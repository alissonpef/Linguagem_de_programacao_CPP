// ls (mostra qual pasta esta)
// cd (nome da pasta/) (vai para qual pasta desejar)
// g++ (nome do arquivo.cpp) -o (nome do arquivo compilado.out)
// g++ (nome da pasta/*.cpp) -o (nome da pasta compilada.out)
// g++ -c (nome do arquivo) para compilar apenas 1 arquivo
// ./(nome do arquivo compilada.out)
// cntrl l ou clear limpa
// cntrl j abre o termimal